# Unset by default - the client derives the API host from whatever host the page was loaded
# from (see src/api/client.ts). Set this only to point the dev frontend at a non-local
# backend (e.g. staging), or to override the auto-derived host/port.
# VITE_API_BASE_URL=http://localhost:3000
